import 'package:flutter/material.dart';

class Aarti extends StatelessWidget {
  const Aarti({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Aarti")),
      body: Column(children: []),
    );
  }
}
